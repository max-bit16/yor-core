Read ~/.claude/skills/impeccable/SKILL.md then audit and
fix every quality issue across the full codebase.

STRICT CONSTRAINTS — do not touch under any circumstances:
- Color values and tokens
- Typography (font families, sizes, weights, line-heights)
- Layout structure and section order
- Animations and transitions
- Background images on competence pages
- Component hierarchy and page structure

Fix ONLY:
- Spacing inconsistencies (padding/margin that breaks the grid)
- Alignment issues between equivalent elements
- Missing or broken :hover / :focus states
- Duplicate or redundant CSS rules
- Unused class names and dead code
- Hardcoded values that should reference an existing token
- Mobile overflow or clipping issues
- Missing alt attributes or aria labels
